server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # 解决 Vue 刷新 404 —— 核心配置
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 安全配置
    location ~ /\.(?!well-known).* {
        deny all;
    }
}